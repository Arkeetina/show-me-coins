import React from 'react';

const Header = () => (
  <header className="header-section">
    <div className="container header-container">
      <h1>Crypto Historic Calculator</h1>
    </div>
  </header>
);

export default Header;
